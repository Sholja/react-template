import Dashboard from '../app/dashboard/components/dashboard';

// possible routes inside the app
const routes = {
  home: {
    path: '/',
    component: Dashboard,
  },
};

export default {
  routes,
};
